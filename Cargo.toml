[package]
name = "embedding-app"
version = "0.1.0"
edition = "2021"

[dependencies]
# GUI
eframe = "0.27"
egui = "0.27"

# File handling
rfd = "0.14"

# Utilities
anyhow = "1.0"
thiserror = "1.0"

# (Optional) ML deps can be added back via a feature when needed
# tokenizers = "0.19"